import 'package:flutter/material.dart';

class UpdateTaskPage extends StatelessWidget {
  const UpdateTaskPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("UpdateTask"),
      ),
    );
  }
}
