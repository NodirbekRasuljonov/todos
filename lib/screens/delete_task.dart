import 'package:flutter/material.dart';

class DeleteTaskPage extends StatelessWidget {
  const DeleteTaskPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Delete Task Page"),
      ),
    );
  }
}
